interface Props {
  open: boolean;
  onClose: () => void;
}

// eslint-disable-next-line
export default function AddCakeModal({ open, onClose }: Props) {
  // eslint-disable-next-line
  return <></>;
}
